# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
supabase==2.7.4

# AI / LLM
anthropic==0.34.2
google-generativeai>=0.8.0
openai>=1.0.0

# Embeddings (BGE-M3 로컬)
# ⚠️  torch는 CUDA 버전에 맞게 별도 설치 필요:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
sentence-transformers>=3.0.0
torch>=2.0.0
accelerate>=0.26.0

# Embedding (로컬)
FlagEmbedding>=1.2.0

# Agent Orchestration
langgraph==0.2.28
langchain-core==0.3.10

# Scheduler
apscheduler==3.10.4

# HTTP Client
httpx==0.27.2

# Email
aiosmtplib==3.0.1

# Config
pydantic==2.9.2
pydantic-settings==2.5.2

# PDF Parsing
pdfplumber==0.11.4

# PDF Generation (Markdown → PDF)
markdown==3.7
reportlab==4.2.5

# PDF Form Filling / 좌표 오버레이
pypdf==4.3.1
pymupdf==1.27.2

# 서류 검토 — 파싱
easyocr>=1.7.0          # 이미지·스캔 PDF OCR (로컬 모델)
python-docx>=1.1.0      # DOCX 텍스트 추출

# HWP Parsing (OLE2 직접 파싱 — pyhwp는 Python 3.12 미지원)
olefile==0.47

# HTML Parsing (bizinfo 상세 페이지 스크래핑)
beautifulsoup4==4.12.3
lxml==5.3.0

# CSV / Excel 파싱 (POS 매출 파일 가져오기)
pandas>=2.2.0
openpyxl>=3.1.0

# Dev / Test
pytest==8.3.3
pytest-asyncio==0.24.0
